import { FormEvents } from "../../modules"

export const EventsAddPage= () => {
    return (
        <section className="flex flex-col h-full w-full px-4 ">
            <FormEvents />
        </section>
    )
}